# Normalize line endings so the repo is consistent regardless of contributor OS.
* text=auto eol=lf

# Binary — never normalize.
*.pdf   binary
*.png   binary
*.jpg   binary
*.webm  binary
*.zip   binary
